# PythonAutomationMastersTestScrapingTeasmanion

use python 3.8

run main.py and tgbot.py

first change the config and create the tables by running the file create_tables.py 
and install requirements.txt
